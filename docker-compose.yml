services:
  personal_website:
    build: .
    container_name: personal_website
    restart: unless-stopped
    networks:
      - caddy_net
    environment:
      - RESEND_API_KEY=${RESEND_API_KEY}
      - PULSE_SERVER=tcp:192.168.18.71
      - BELL_EXEC_TIMEOUT_MS=120000
      - DOORBELL_FILE=/app/scripts/sounds/doorbell_sound.mp3
      - HUMAN_DOORBELL_FILE=/app/scripts/sounds/human_doorbell_sound.mp3
      - DOORBELL_REPEAT=3
      - HUMAN_DOORBELL_REPEAT=1
      - PLAYBACK_ROUNDS=2

networks:
  caddy_net:
    external: true

